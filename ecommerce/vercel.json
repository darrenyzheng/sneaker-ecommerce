{
  "version": 2,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/server.js"
    }
  ],
  "build": {
    "commands": [
      "npm install pg",
      "npm install"
    ]
  }
}
